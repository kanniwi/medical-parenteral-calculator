# Исправление Network Error

## Проблема
`localhost` не работает на мобильных устройствах. Нужно использовать реальный IP адрес вашего компьютера.

## Решение

### Шаг 1: Узнайте IP адрес вашего компьютера

В PowerShell или CMD выполните:

```powershell
ipconfig
```

Найдите **IPv4 Address** в секции вашего активного подключения (Wi-Fi или Ethernet).

**Пример вывода:**
```
Адаптер беспроводной локальной сети "Wi-Fi":

   IPv4-адрес. . . . . . . . . . . . : 192.168.1.105
   Маска подсети . . . . . . . . . . : 255.255.255.0
   Основной шлюз. . . . . . . . . . : 192.168.1.1
```

В этом примере ваш IP: **192.168.1.105**

### Шаг 2: Обновите API URL в приложении

Откройте файл `utils/api.ts` и замените IP:

```typescript
const API_BASE_URL = __DEV__
  ? 'http://ВАШ_IP:3000/api'  // Замените ВАШ_IP на найденный IPv4
  : 'https://your-production-server.com/api';
```

**Пример:**
```typescript
const API_BASE_URL = __DEV__
  ? 'http://192.168.1.105:3000/api'
  : 'https://your-production-server.com/api';
```

### Шаг 3: Обновите CORS в backend

Откройте `server/.env` и обновите:

```env
CORS_ORIGIN=http://localhost:8081,exp://ВАШ_IP:8081
```

**Пример:**
```env
CORS_ORIGIN=http://localhost:8081,exp://192.168.1.105:8081
```

### Шаг 4: Перезапустите всё

1. **Остановите сервер** (Ctrl+C)
2. **Перезапустите сервер:**
   ```bash
   cd server
   npm run dev
   ```

3. **Перезапустите приложение** (в другом терминале, из корня проекта):
   ```bash
   npm start
   ```
   Затем нажмите `r` для перезагрузки или `a`/`i` чтобы переоткрыть

### Шаг 5: Проверьте подключение

#### 5.1 Проверьте сервер в браузере

Откройте в браузере на **том же компьютере**:
```
http://ВАШ_IP:3000/health
```

**Пример:**
```
http://192.168.1.105:3000/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "message": "Server is running",
  "database": "connected"
}
```

#### 5.2 Проверьте с мобильного устройства

Откройте браузер **на мобильном устройстве** и перейдите по тому же адресу:
```
http://ВАШ_IP:3000/health
```

Если открылось - подключение работает!

### Шаг 6: Проверьте firewall

Если браузер на мобильном не открывает адрес, нужно разрешить подключения в Windows Firewall:

1. Откройте **Windows Defender Firewall**
2. **Разрешить работу с приложением или компонентом** (Allow an app)
3. Найдите **Node.js** в списке
4. Убедитесь что отмечены **Private** и **Public**
5. Если Node.js нет в списке - нажмите **Разрешить другое приложение** и добавьте `node.exe`

### Шаг 7: Попробуйте зарегистрироваться

1. Откройте приложение
2. Профиль → Зарегистрироваться
3. Введите email, пароль, имя
4. Нажмите "Зарегистрироваться"

**Смотрите логи сервера** - там будет видно что происходит:
```
POST /api/auth/register
📝 Registration attempt: { email: 'test@test.com', name: 'Test' }
🔐 Password hashed
✅ User created: { id: 1, email: 'test@test.com' }
🎫 Token generated
✅ Registration successful for: test@test.com
```

## Возможные проблемы

### Problem: "Network Error" всё ещё появляется

**Причины:**
1. Неправильный IP в `utils/api.ts`
2. Сервер не запущен
3. Firewall блокирует
4. Устройство и компьютер в разных сетях

**Решение:**
- Убедитесь что устройство и компьютер в **одной Wi-Fi сети**
- Проверьте что сервер запущен
- Попробуйте отключить firewall временно для теста
- Проверьте что IP правильный (не 127.0.0.1 и не localhost)

### Problem: Регистрация не происходит, но ошибок нет

**Проверьте логи сервера:**

Если видите:
```
POST /api/auth/register
```
Но дальше ничего - значит запрос не доходит до контроллера.

**Возможные причины:**
- Middleware блокирует
- CORS проблема
- Неправильный формат данных

**Решение:**
Проверьте логи подробнее и отправьте мне полный вывод.

### Problem: "User already exists"

Пользователь с таким email уже есть в базе.

**Решение:**
- Используйте другой email
- Или удалите пользователя из БД:
  ```sql
  psql -U postgres -d calculator_db
  DELETE FROM users WHERE email = 'ваш@email.com';
  ```

## Тестирование с Postman (альтернатива)

Если хотите протестировать API напрямую:

### Регистрация
```
POST http://ВАШ_IP:3000/api/auth/register
Content-Type: application/json

{
  "email": "test@test.com",
  "password": "password123",
  "name": "Test User"
}
```

### Health Check
```
GET http://ВАШ_IP:3000/health
```

### Получение расчетов (без токена - гостевой режим)
```
GET http://ВАШ_IP:3000/api/calculations
```

## Быстрая проверка

Выполните эти команды по порядку:

```powershell
# 1. Найти IP
ipconfig | findstr IPv4

# 2. Проверить что сервер запущен
curl http://localhost:3000/health

# 3. Проверить с вашего IP
curl http://ВАШ_IP:3000/health
```

Все команды должны вернуть JSON с `"status": "ok"`.

## Итого

1. ✅ Найдите свой IP: `ipconfig`
2. ✅ Замените в `utils/api.ts`: `http://ВАШ_IP:3000/api`
3. ✅ Обновите `.env`: `CORS_ORIGIN=...,exp://ВАШ_IP:8081`
4. ✅ Перезапустите сервер и приложение
5. ✅ Проверьте health check в браузере
6. ✅ Попробуйте зарегистрироваться

Если проблемы остались - покажите мне логи сервера и ошибку из приложения!
