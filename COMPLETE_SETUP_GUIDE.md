# Полное руководство по настройке приложения

## Обзор

Приложение Medical Parenteral Calculator теперь поддерживает **гостевой режим** - пользователи могут использовать калькулятор без регистрации. История сохраняется только для зарегистрированных пользователей.

## Часть 1: Настройка PostgreSQL

### 1. Проверьте, что PostgreSQL установлен и запущен

```bash
# Windows
# Откройте Services и найдите PostgreSQL
# Или в PowerShell:
Get-Service postgresql*

# Проверка версии
psql --version
```

### 2. База данных уже создана

Вы создали базу `calculator_db` с пользователем `postgres` и паролем `postgres`.

Если нужно создать заново:
```sql
psql -U postgres
CREATE DATABASE calculator_db;
\q
```

## Часть 2: Настройка Backend (Node.js + Express)

### 1. Перейдите в папку server

```bash
cd server
```

### 2. Установите зависимости

```bash
npm install
```

Установленные пакеты:
- `express` - веб-фреймворк
- `pg` - PostgreSQL клиент
- `bcryptjs` - хеширование паролей
- `jsonwebtoken` - JWT токены
- `cors` - CORS middleware
- `dotenv` - переменные окружения
- `express-validator` - валидация данных

### 3. Файл .env уже создан

Проверьте `server/.env`:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=calculator_db
DB_USER=postgres
DB_PASSWORD=postgres

PORT=3000
NODE_ENV=development

JWT_SECRET=medical_calculator_secret_key_2024_change_in_production
JWT_EXPIRES_IN=7d

CORS_ORIGIN=http://localhost:8081,exp://192.168.1.100:8081
```

### 4. Запустите миграцию базы данных

```bash
npm run migrate
```

**Ожидаемый вывод:**
```
🔄 Starting database migration...
✅ Users table created
✅ Calculations table created
✅ Indexes created
✅ Triggers created
🎉 Database migration completed successfully!
```

### 5. Запустите сервер

**Режим разработки (с автоперезагрузкой):**
```bash
npm run dev
```

**Продакшн режим:**
```bash
npm start
```

**Ожидаемый вывод:**
```
✅ Connected to PostgreSQL database
🚀 Server is running on port 3000
📊 Environment: development
🗄️  Database: calculator_db
🌐 CORS: http://localhost:8081,exp://192.168.1.100:8081
```

### 6. Проверьте работу API

Откройте в браузере или Postman:
```
http://localhost:3000/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "Server is running",
  "database": "connected"
}
```

## Часть 3: Настройка Mobile App (React Native + Expo)

### 1. Вернитесь в корневую папку проекта

```bash
cd ..
```

### 2. Убедитесь, что зависимости установлены

```bash
npm install
```

### 3. Обновите API endpoint в приложении

Откройте `utils/api.ts` и убедитесь, что BASE_URL указывает на ваш сервер:

```typescript
const BASE_URL = 'http://localhost:3000/api';
// Или для физического устройства:
// const BASE_URL = 'http://192.168.1.XXX:3000/api';
```

### 4. Запустите приложение

```bash
npm start
```

Или:
```bash
npx expo start
```

### 5. Откройте приложение

- **iOS Simulator:** Нажмите `i` в терминале
- **Android Emulator:** Нажмите `a` в терминале
- **Физическое устройство:** Отсканируйте QR-код в Expo Go

## Гостевой режим - Как работает

### Для гостей (без входа):
✅ **Доступно:**
- Полный доступ к калькулятору
- Выполнение всех расчетов
- Просмотр результатов

❌ **Недоступно:**
- Сохранение истории расчетов
- Синхронизация между устройствами
- Профиль пользователя

### Для зарегистрированных пользователей:
✅ **Всё доступно:**
- Калькулятор
- Сохранение истории (до 50 последних)
- Синхронизация через облако
- Профиль с данными
- Доступ с любого устройства

## API Endpoints

### Аутентификация (опциональна)

**Регистрация:**
```http
POST http://localhost:3000/api/auth/register
Content-Type: application/json

{
  "email": "test@example.com",
  "password": "password123",
  "name": "Test User"
}
```

**Вход:**
```http
POST http://localhost:3000/api/auth/login
Content-Type: application/json

{
  "email": "test@example.com",
  "password": "password123"
}
```

**Профиль (требует токен):**
```http
GET http://localhost:3000/api/auth/profile
Authorization: Bearer <your-token-here>
```

### Расчеты

**Создать расчет (работает БЕЗ токена для гостей):**
```http
POST http://localhost:3000/api/calculations
Content-Type: application/json

{
  "glucose_volume": 500,
  "glucose_concentration": 10,
  "amino_acids_volume": 100,
  "amino_acids_concentration": 10,
  "lipids_volume": 250,
  "lipids_concentration": 20,
  "glucose_grams": 50,
  "glucose_calories": 170,
  "amino_acids_grams": 10,
  "amino_acids_calories": 40,
  "lipids_grams": 50,
  "lipids_calories": 500,
  "total_calories": 710,
  "total_volume": 850
}
```

**Получить историю:**
```http
GET http://localhost:3000/api/calculations
# Без токена - вернет пустой массив
# С токеном - вернет все расчеты пользователя
Authorization: Bearer <your-token-here>
```

## Структура базы данных

### Таблица users
```sql
id              SERIAL PRIMARY KEY
email           VARCHAR(255) UNIQUE NOT NULL
password_hash   VARCHAR(255) NOT NULL
name            VARCHAR(255)
created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
```

### Таблица calculations
```sql
id                           SERIAL PRIMARY KEY
user_id                      INTEGER (nullable для гостей)
glucose_volume               DECIMAL(10, 2) NOT NULL
glucose_concentration        DECIMAL(10, 2) NOT NULL
amino_acids_volume           DECIMAL(10, 2) NOT NULL
amino_acids_concentration    DECIMAL(10, 2) NOT NULL
lipids_volume                DECIMAL(10, 2) NOT NULL
lipids_concentration         DECIMAL(10, 2) NOT NULL
glucose_grams                DECIMAL(10, 2) NOT NULL
glucose_calories             DECIMAL(10, 2) NOT NULL
amino_acids_grams            DECIMAL(10, 2) NOT NULL
amino_acids_calories         DECIMAL(10, 2) NOT NULL
lipids_grams                 DECIMAL(10, 2) NOT NULL
lipids_calories              DECIMAL(10, 2) NOT NULL
total_calories               DECIMAL(10, 2) NOT NULL
total_volume                 DECIMAL(10, 2) NOT NULL
created_at                   TIMESTAMP DEFAULT CURRENT_TIMESTAMP
```

## Troubleshooting

### Проблема: Сервер не запускается

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте данные в `.env`
3. Убедитесь, что порт 3000 свободен

### Проблема: База данных не подключается

**Решение:**
```bash
# Проверьте подключение вручную
psql -U postgres -d calculator_db

# Если не работает - проверьте пароль
# Если нужно, измените пароль:
psql -U postgres
ALTER USER postgres WITH PASSWORD 'postgres';
```

### Проблема: Миграция не создает таблицы

**Решение:**
```bash
# Удалите таблицы и запустите миграцию заново
psql -U postgres -d calculator_db
DROP TABLE IF EXISTS calculations CASCADE;
DROP TABLE IF EXISTS users CASCADE;
\q

npm run migrate
```

### Проблема: Приложение не подключается к API

**Решение:**
1. Убедитесь, что сервер запущен (`http://localhost:3000/health`)
2. Проверьте `BASE_URL` в `utils/api.ts`
3. Для физического устройства используйте IP компьютера, а не `localhost`
4. Проверьте CORS настройки в `server/.env`

### Проблема: CORS ошибки

**Решение:**
Обновите `CORS_ORIGIN` в `server/.env`:
```env
CORS_ORIGIN=http://localhost:8081,exp://ВАШ_IP:8081
```

## Тестирование

### 1. Тестирование гостевого режима

1. Откройте приложение
2. Перейдите на вкладку "Калькулятор"
3. Введите данные и нажмите "Рассчитать"
4. Результаты должны отобразиться
5. Перейдите на вкладку "История" - будет пусто с сообщением
6. Перейдите на "Профиль" - увидите кнопки входа/регистрации

### 2. Тестирование с регистрацией

1. На вкладке "Профиль" нажмите "Зарегистрироваться"
2. Введите email, пароль и имя
3. После регистрации вы будете автоматически вошли
4. Сделайте несколько расчетов
5. Перейдите на "История" - расчеты сохранены
6. Профиль показывает ваши данные

### 3. Проверка базы данных

```sql
psql -U postgres -d calculator_db

-- Посмотреть пользователей
SELECT * FROM users;

-- Посмотреть расчеты
SELECT id, user_id, total_calories, created_at FROM calculations ORDER BY created_at DESC LIMIT 10;

-- Расчеты гостей (user_id IS NULL)
SELECT COUNT(*) FROM calculations WHERE user_id IS NULL;

-- Расчеты пользователей
SELECT COUNT(*) FROM calculations WHERE user_id IS NOT NULL;
```

## Следующие шаги

1. ✅ База данных настроена
2. ✅ Backend запущен
3. ✅ Mobile app работает
4. ✅ Гостевой режим активен

**Готово к использованию!**

Теперь пользователи могут:
- Использовать калькулятор без регистрации
- Зарегистрироваться для сохранения истории
- Синхронизировать данные между устройствами

## Безопасность (для продакшн)

⚠️ **Важно перед развертыванием:**

1. Измените `JWT_SECRET` на случайную строку
2. Используйте HTTPS для API
3. Настройте firewall для PostgreSQL
4. Используйте переменные окружения, а не хардкод
5. Настройте rate limiting
6. Добавьте логирование

## Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Проверьте логи PostgreSQL
3. Проверьте Metro bundler в Expo
4. Откройте Developer Tools в приложении (встряхните устройство)
